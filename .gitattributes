# Windows batch files must be CRLF so cmd.exe parses them correctly on every PC
# (LF-only .bat can mis-parse). Keep .bat ASCII-only as well.
*.bat text eol=crlf

# Normalize other text to LF in the repo
*.py   text eol=lf
*.md   text
